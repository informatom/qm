<%= form_for(@substitution) do |f| %>
  <% if @substitution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@substitution.errors.count, "error") %> prohibited this substitution from being saved:</h2>

      <ul>
      <% @substitution.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :deputy_id %><br />
    <%= f.number_field :deputy_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

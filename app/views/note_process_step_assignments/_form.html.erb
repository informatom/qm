<%- model_class = NoteProcessStepAssignment -%>
<%= bootstrap_error(@note_process_step_assignment, model_class ) %>

<%= simple_form_for @note_process_step_assignment, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :note_id, :collection => Note.where(:company_id => current_company.id) %>
  <%= f.input :process_step_id, :collection => ProcessStep.where(:company_id => current_company.id) %>
  <%= company_field(@note_process_step_assignment, f) %>

  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                note_process_step_assignments_path, :class => 'btn' %>
  </div>
<% end %>

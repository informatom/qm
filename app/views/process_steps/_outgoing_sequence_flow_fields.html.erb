<div class="nested-fields">
  <%= f.input :target_id, :required => false do %>
    <%= f.input_field :target_id, :collection => ProcessStep.where(:company_id => current_company.id) %>
    <%= link_to_remove_association t("helpers.remove_assignment"), f, :class => "btn btn-small btn-danger" %>
  <% end %>
  <%= f.hidden_field :company_id, value: current_company.id %>
  <%= f.hidden_field :flow_object_id, value: FlowObject.where(name: "SequenceFlow").first.id %>
</div>

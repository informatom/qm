<%= form_for(@role_in_company) do |f| %>
  <% if @role_in_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@role_in_company.errors.count, "error") %> prohibited this role_in_company from being saved:</h2>

      <ul>
      <% @role_in_company.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :company_id %><br />
    <%= f.number_field :company_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
